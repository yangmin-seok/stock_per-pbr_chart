# 에이전트 협업 가이드 (AGENTS.md)

이 문서는 AI 시스템과 사람간의 효과적인 협업을 위한 역할을 규정합니다.

### 1. Auth Agent
- **역할**: `pykrx`의 핵심 통신 로직 몽키패칭 및 인증 세션 관리.
- **책임**: `pykrx.website.comm.webio`의 Get/Post read 방식을 requests.Session()으로 덮어씌워 인증 세션을 유지합니다. KRX 서버 특성상 로그인 우회, 중복 로그인 등에 대한 오류 처리 정책(`continue`, `raise`)을 제어합니다.

### 2. Data Agent
- **역할**: pykrx 메서드를 활용하여 데이터 수집 및 10년치 시계열 정제.
- **책임**: 과거 10년간의 KOSPI/KOSDAQ 주가, BPS/EPS 등 데이터를 패치된 Auth 라이브러리를 통해 호출하며, 스크래핑 제재나 속도 저하를 피하도록 호출 빈도 및 장 마감 후 업데이트(1일 1회) 스케줄링 로직을 구축합니다. SQLite에 이 정보를 파싱해 보관(캐싱)합니다.

### 3. UI Agent
- **역할**: 인터랙티브 프론트엔드 및 데이터 시각화 구성.
- **책임**: `Streamlit`을 구동하고, PER/PBR 밴드 차트를 표현하기 위해 `Plotly`를 이용하여 종목별 고평가/저평가 섹터 시각화를 수행합니다. 검색 필터와 산점도 기능을 지원합니다.
