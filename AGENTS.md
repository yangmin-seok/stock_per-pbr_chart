# 에이전트 협업 가이드 (AGENTS.md)

이 문서는 AI 시스템과 사람간의 효과적인 협업을 위한 역할을 규정합니다.

### 1. Auth Agent
- **역할**: `pykrx`의 핵심 통신 로직 몽키패칭 및 인증 세션 관리.
- **책임**: `pykrx.website.comm.webio`의 Get/Post read 방식을 requests.Session()으로 덮어씌워 인증 세션을 유지합니다. KRX 서버 특성상 로그인 우회, 중복 로그인 등에 대한 오류 처리 정책(`continue`, `raise`)을 제어합니다.

### 2. Data Agent
- **역할**: pykrx 및 yfinance 등 다양한 소스를 활용하여 시장 데이터, 매크로 지표, 10년치 시계열 정제.
- **책임**: `data_agent.py`를 통해 Streamlit 구동 시 백그라운드 데몬 스레드로 동작합니다. 주기적(1시간 단위 등)으로 시장이 업데이트되었는지 확인하며, KOSPI/KOSDAQ 전 종목에 대한 10년치 통계 및 환율, 유가, 미국 지수 등의 매크로 지표를 SQLite DB에 선제적으로 적재(Pre-fetch)합니다. 앱 화면 렌더링 시 발생하는 지연(Blocking)을 방지하는 아키텍처의 핵심 역할을 수행합니다.

### 3. UI Agent
- **역할**: 인터랙티브 프론트엔드 및 데이터 시각화 구성.
- **책임**: `Streamlit`을 구동하여 종목별 PER/PBR 밴드 차트, 섹터/YTD 파이 및 트리맵, 수익률 비교 등 다양한 Plotly 시각화를 사용자에게 제공합니다. 앱은 렌더링 시 외부 API가 아닌 로컬 SQLite DB부터 우선 조회하여 0.1초 수준의 빠른 반응 속도를 보장합니다.
